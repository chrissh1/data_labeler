# Copy to .env, replace placeholders, then load it using the README commands.
# Do not include the trailing period from the chat message in the database name.
# URL-encode special characters in the password, such as @ -> %40 and # -> %23.
DATABASE_URL='postgresql://postgres.grtffvszvltdfedmeloe:YOUR_URL_ENCODED_PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require'

# Exactly 64 hex characters. Export the existing .email_hash_key using the README
# command to preserve participant identities. Keep this value stable and private.
EMAIL_HASH_KEY='YOUR_64_CHARACTER_HEX_KEY'
